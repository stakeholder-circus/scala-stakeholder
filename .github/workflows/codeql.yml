name: codeql

on:
  push:
  pull_request:
  schedule:
    - cron: '19 3 * * 1'

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CodeQL has no selected first-class Scala analysis lane in this repo; native and Docker workflows are authoritative."
